#!/usr/bin/env python
# -*- coding: utf-8 -*-

# AUTOMATICALLY MADE FILE. DO NOT EDIT.
# This file was generated by hans on 2022-09-10 with Python executable
# /home/hans/webviz/webviz-api-experiments/venv/bin/python

import logging
import logging.config
import os
import threading
import datetime
from pathlib import Path, PosixPath, WindowsPath

from uuid import uuid4

from dash import html, dcc, Dash, Input, Output, callback, callback_context
import webviz_core_components as wcc
from flask_talisman import Talisman
import webviz_config
import webviz_config.plugins
from webviz_config.themes import installed_themes
from webviz_config.common_cache import CACHE
from webviz_config.webviz_store import WEBVIZ_STORAGE
from webviz_config.webviz_assets import WEBVIZ_ASSETS
from webviz_config.webviz_instance_info import WebvizRunMode, WEBVIZ_INSTANCE_INFO
from webviz_config.webviz_factory_registry import WEBVIZ_FACTORY_REGISTRY
from webviz_config.utils import deprecate_webviz_settings_attribute_in_dash_app

import webviz_core_components as wcc


# Start out by setting a sensible configuration for the root logger and setting a global 
# loglevel. The (global) loglevel defaults to WARNING, but can be set by the user via 
# the --loglevel argument. The call to basicConfig() should happen before any other logging 
# related calls, see https://docs.python.org/3/library/logging.html#logging.basicConfig
logging.basicConfig(level=logging.WARNING)

theme = webviz_config.WebvizConfigTheme("default")
theme.from_json((Path(__file__).resolve().parent / "theme_settings.json").read_text())

app = Dash(
    name=__name__,
    external_stylesheets=theme.external_stylesheets,
    requests_pathname_prefix="/dash/",
    assets_folder=Path("resources") / "assets",
    meta_tags=[
        {"name": "viewport", "content": "width=device-width, initial-scale=1"}
    ],
)

# For signing session cookies
# Use user provided environment variable if exists, otherwise generate fresh
app.server.secret_key = os.environ.get("WEBVIZ_SESSION_SECRET_KEY") or webviz_config.LocalhostToken.generate_token()

server = app.server

app.title = "Reek Webviz Demonstration"
app.config.suppress_callback_exceptions = True

# Dash is very eager to configure its own logger by both adding a handler and hardcoding
# the loglevel to INFO. Below we counteract both of these.
# We do not want to show INFO regarding werkzeug routing as that is too verbose.
app.logger.setLevel(logging.WARNING)
app.logger.handlers.clear()
logging.getLogger("werkzeug").setLevel(logging.WARNING)



# Create the common webviz_setting object that will get passed as an
# argument to all plugins that request it.
webviz_settings: webviz_config.WebvizSettings = webviz_config.WebvizSettings(
    shared_settings=webviz_config.SHARED_SETTINGS_SUBSCRIPTIONS.transformed_settings(
        {}, PosixPath('/home/hans/webviz/webviz-config/examples'), True 
    ),
    theme=theme,
)

# Previously, webviz_settings was piggybacked onto the Dash application object.
# For a period of time, keep it but mark access to the webviz_settings attribute
# on the Dash application object as deprecated.
deprecate_webviz_settings_attribute_in_dash_app()
app._deprecated_webviz_settings = {
    "shared_settings" : webviz_settings.shared_settings,
    "theme" : webviz_settings.theme,
    "portable" : True,
}

CACHE.init_app(server)

storage_folder = Path(__file__).resolve().parent / "resources" / "webviz_storage"

WEBVIZ_STORAGE.use_storage = True
WEBVIZ_STORAGE.storage_folder = storage_folder

WEBVIZ_ASSETS.portable = True

run_mode = WebvizRunMode.PORTABLE if True else WebvizRunMode.NON_PORTABLE
WEBVIZ_INSTANCE_INFO.initialize(
    dash_app=app,
    run_mode=run_mode,
    theme=theme,
    storage_folder=storage_folder
)

WEBVIZ_FACTORY_REGISTRY.initialize(None)

use_oauth2 = False

if False and not webviz_config.is_reload_process():
    # When Dash/Flask is started on localhost with hot module reload activated,
    # we do not want the main process to call expensive component functions in
    # the layout tree, as the layout tree used on initialization will anyway be called
    # from the child/restart/reload process.
    app.layout = html.Div()
else:
    count_text_plugins = 0
    page_plugins = {}
    page_settings = {}
    
    page_plugins["front-page"] = []
    page_settings["front-page"] = []
    
    plugin = webviz_config.plugins.BannerImage(**{'image': PosixPath('/home/hans/webviz/webviz-config/examples/example_banner.png'), 'title': 'My banner image', 'color': 'white', 'shadow': True, 'height': 300, 'title_position': 'center'})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["front-page"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["front-page"].extend([settings for settings in plugin.get_all_settings()])

    
    
    page_plugins["front-page"].append(wcc.WebvizPluginWrapper(id=f"text-{count_text_plugins}", views=[], initiallyActiveViewId="", name="Text", children=[dcc.Markdown(r"""Webviz created from configuration file.""")]))
    count_text_plugins += 1
    
    
    page_plugins["front-page"].append(wcc.WebvizPluginWrapper(id=f"text-{count_text_plugins}", views=[], initiallyActiveViewId="", name="Text", children=[dcc.Markdown(r"""Some other text, potentially with strange letters like Åre, Smørbukk Sør.""")]))
    count_text_plugins += 1
    
    
    
    page_plugins["markdown-example"] = []
    page_settings["markdown-example"] = []
    
    plugin = webviz_config.plugins.Markdown(**{'markdown_file': PosixPath('/home/hans/webviz/webviz-config/examples/example-markdown.md')})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["markdown-example"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["markdown-example"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["table-example"] = []
    page_settings["table-example"] = []
    
    plugin = webviz_config.plugins.DataTable(**{'csv_file': PosixPath('/home/hans/webviz/webviz-config/examples/example_data.csv'), 'sorting': True, 'filtering': True, 'pagination': True})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["table-example"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["table-example"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["pdf-example"] = []
    page_settings["pdf-example"] = []
    
    plugin = webviz_config.plugins.EmbedPdf(**{'pdf_file': PosixPath('/home/hans/webviz/webviz-config/examples/example.pdf'), 'height': 80, 'width': 100})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["pdf-example"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["pdf-example"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["syntax-highlighting-example"] = []
    page_settings["syntax-highlighting-example"] = []
    
    plugin = webviz_config.plugins.SyntaxHighlighter(**{'filename': PosixPath('/home/hans/webviz/webviz-config/examples/basic_example.yaml'), 'dark_theme': False})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["syntax-highlighting-example"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["syntax-highlighting-example"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["plot-a-table"] = []
    page_settings["plot-a-table"] = []
    
    plugin = webviz_config.plugins.TablePlotter(app=app, webviz_settings=webviz_settings, **{'csv_file': PosixPath('/home/hans/webviz/webviz-config/examples/example_data.csv'), 'filter_cols': ['Well', 'Segment', 'Average permeability (D)'], 'plot_options': {'type': 'bar', 'facet_col': 'Well', 'color': 'Segment', 'barmode': 'group'}, 'filter_defaults': {'Well': ['A-1H', 'A-2H', 'C-1H']}, 'column_color_discrete_maps': {'Segment': {'A': '#ff0000', 'B': 'rgb(0,255,0)', 'C': 'blue'}}, 'lock': False})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["plot-a-table"].extend(plugin.plugin_layout(contact_person={'name': 'Ola Nordmann', 'phone': '+47 12345678', 'email': 'some@email.com'}, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["plot-a-table"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["plot-a-table-locked"] = []
    page_settings["plot-a-table-locked"] = []
    
    plugin = webviz_config.plugins.TablePlotter(app=app, webviz_settings=webviz_settings, **{'csv_file': PosixPath('/home/hans/webviz/webviz-config/examples/example_data.csv'), 'lock': True, 'plot_options': {'x': 'Well', 'y': 'Initial reservoir pressure (bar)', 'size': 'Average permeability (D)', 'facet_col': 'Segment'}, 'filter_cols': None, 'filter_defaults': None, 'column_color_discrete_maps': None})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["plot-a-table-locked"].extend(plugin.plugin_layout(contact_person={'name': 'Kari Nordmann', 'phone': 12345678, 'email': 'someother@email.com'}, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["plot-a-table-locked"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    page_plugins["pivot-table"] = []
    page_settings["pivot-table"] = []
    
    plugin = webviz_config.plugins.PivotTable(**{'csv_file': PosixPath('/home/hans/webviz/webviz-config/examples/example_data.csv'), 'options': {'cols': ['Well'], 'rows': ['Segment'], 'vals': ['Average permeability (D)'], 'aggregatorName': 'Average', 'rendererName': 'Table Heatmap'}})
    if not use_oauth2:
        use_oauth2 = plugin.oauth2 if hasattr(plugin, "oauth2") else use_oauth2

    page_plugins["pivot-table"].extend(plugin.plugin_layout(contact_person=None, plugin_deprecation_warnings=[], argument_deprecation_warnings=[]))
    page_settings["pivot-table"].extend([settings for settings in plugin.get_all_settings()])

    
    
    
    app.layout = html.Div(
        className="layoutWrapper", 
        children=[
            dcc.Location(id='location', refresh=True),
            wcc.WebvizContentManager(
                id="webviz-content-manager",
                children=[
                    wcc.Menu(
                        id="main-menu",
                        showLogo=True,
                        menuBarPosition="left",
                        menuDrawerPosition="left",
                        initiallyPinned=True,
                        initiallyCollapsed=False,
                        navigationItems=[{'type': 'page', 'title': 'Front page', 'href': '/dash/front-page', 'icon': None}, {'type': 'page', 'title': 'Markdown example', 'href': '/dash/markdown-example', 'icon': None}, {'type': 'page', 'title': 'Table example', 'href': '/dash/table-example', 'icon': None}, {'type': 'page', 'title': 'PDF example', 'href': '/dash/pdf-example', 'icon': None}, {'type': 'page', 'title': 'Syntax highlighting example', 'href': '/dash/syntax-highlighting-example', 'icon': None}, {'type': 'page', 'title': 'Plot a table', 'href': '/dash/plot-a-table', 'icon': None}, {'type': 'page', 'title': 'Plot a table (locked)', 'href': '/dash/plot-a-table-locked', 'icon': None}, {'type': 'page', 'title': 'Pivot Table', 'href': '/dash/pivot-table', 'icon': None}],
                    ),
                    wcc.WebvizSettingsDrawer(
                        id="settings-drawer",
                        children=[],
                    ),
                    wcc.WebvizPluginsWrapper(
                        id="plugins-wrapper",
                        children=[],
                    )
                ]
            ),
        ]
    )

WEBVIZ_FACTORY_REGISTRY.cleanup_resources_after_plugin_init()

theme.adjust_csp({"script-src": app.csp_hashes()}, append=True)
Talisman(server, content_security_policy=theme.csp, feature_policy=theme.feature_policy, force_https=False, session_cookie_secure=False)

oauth2 = webviz_config.Oauth2(app.server) if use_oauth2 else None

@callback(
    Output("plugins-wrapper", "children"), 
    Output("settings-drawer", "children"), 
    Input("main-menu", "url"), 
    Input("location", "pathname")
)
def update_page(url, pathname):
    ctx = callback_context
    pathname = pathname.replace("/dash/","")
    if ctx.triggered:
        if ctx.triggered[0]["prop_id"].split('.')[0] == "main-menu":
            pathname = url.replace("/dash/", "") if url is not None else ""
        else:
            if pathname is not None:
                pathname = pathname.replace("/", "")
            else:
                pathname = ""
        if not pathname:
            pathname = next(iter(page_plugins)) 
    
    return page_plugins.get(pathname, ["Oooppss... Page not found."]), page_settings.get(pathname, [])



if __name__ == "__main__":
    # This part is ignored when the webviz app is started
    # using Docker container and uwsgi (e.g. when hosted on Azure).
    #
    # It is used only when directly running this script with Python,
    # which will then initialize a localhost server.

    port = webviz_config.utils.get_available_port(preferred_port=5000)

    token = webviz_config.LocalhostToken(app.server, port, oauth2).one_time_token
    webviz_config.utils.LocalhostOpenBrowser(port, token)

    webviz_config.utils.silence_flask_startup()

    app.run_server(
        host="localhost",
        port=port,
        debug=False,
        use_reloader=False,
        dev_tools_prune_errors=False,
        
                
    )
else:
    # This will be applied if not running on localhost
    if use_oauth2:
        oauth2.set_oauth2_before_request_decorator()